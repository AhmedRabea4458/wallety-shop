name: drift_test
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Drift + SQLite for Flutter
  drift: ^2.26.0
  drift_flutter: ^0.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # Drift code generation
  drift_dev: ^2.26.0
  build_runner: ^2.4.15

  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
